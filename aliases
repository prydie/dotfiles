# vim: ft=sh

alias clipboard="perl -pe 'chomp if eof' | pbcopy 2> /dev/null || xclip -sel clip"
alias vimf='vim $(fzf)'

function rsa-fingerprint() {
     openssl rsa -pubout -outform DER -in $1 2> /dev/null | openssl md5 -c
}
