#include <stdio.h>

/* Target filesystem */
typedef struct fsTarget {
    char *target;
    int port;
}

struct fsTarget *lookup(char *target) {
    struct *fsTarget = null;
}

main() {
    printf(addr);
}
